import App from './App.svelte';
import 'node_modules/bootstrap/dist/css/bootstrap.min.css'

const app = new App({
	target: document.body,
	// data we pass to the app component is here
});

export default app;